org.gradle.jvmargs=-Xmx1536M
android.useAndroidX=true
android.enableJetifier=true
# Disable Kotlin incremental to avoid "different roots" when project (E:) and pub cache (C:) are on different drives
kotlin.incremental=false