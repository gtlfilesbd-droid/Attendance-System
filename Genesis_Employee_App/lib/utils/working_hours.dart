/// Time limit disabled: tracking runs whenever user presses Start duty until End duty.
/// No plugin dependencies so unit tests can run without loading native code.
bool isWorkingHours([DateTime? now]) {
  return true;
}
